apiVersion: v1
kind: Service
metadata:
  name: channel
  namespace: apps
  labels:
    app: channel
    app.kubernetes.io/part-of: chat-system
spec:
  type: ClusterIP
  selector:
    app: channel
  ports:
    - name: http
      port: 3000
      targetPort: http
