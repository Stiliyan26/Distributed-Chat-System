apiVersion: v1
kind: Secret
metadata:
  name: delivery-secret
  namespace: apps
  labels:
    app: delivery
    app.kubernetes.io/part-of: chat-system
type: Opaque
stringData:
  SMTP_USER: local-dev@example.com
  SMTP_PASS: local-dev-placeholder
