apiVersion: v1
kind: Service
metadata:
  name: kafka-hl
  namespace: infra
  labels:
    app: kafka
    app.kubernetes.io/part-of: chat-system
spec:
  clusterIP: None
  selector:
    app: kafka
  ports:
    - name: broker
      port: 9092
      targetPort: broker
---
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: infra
  labels:
    app: kafka
    app.kubernetes.io/part-of: chat-system
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - name: broker
      port: 9092
      targetPort: broker
