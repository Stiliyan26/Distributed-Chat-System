apiVersion: v1
kind: Service
metadata:
  name: zookeeper-hl
  namespace: infra
  labels:
    app: zookeeper
    app.kubernetes.io/part-of: chat-system
spec:
  clusterIP: None
  selector:
    app: zookeeper
  ports:
    - name: client
      port: 2181
      targetPort: client
---
apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: infra
  labels:
    app: zookeeper
    app.kubernetes.io/part-of: chat-system
spec:
  type: ClusterIP
  selector:
    app: zookeeper
  ports:
    - name: client
      port: 2181
      targetPort: client
